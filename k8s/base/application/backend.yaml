# ============================================================
# BACKEND (.NET 8 API) - Deployment + Service
# ============================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: questionario
  labels:
    app: backend
spec:
  replicas: 4                  # 4 instâncias da API (requisito do trabalho!)
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: luqui25/lucas-fluminense-backend:latest   # Sua imagem do DockerHub
          ports:
            - containerPort: 8080        # .NET 8 escuta na 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Development"
            # Connection string vem direto do Secret
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: questionario-secrets
                  key: MONGO_CONNECTION_STRING
            - name: MongoDB__DatabaseName
              valueFrom:
                secretKeyRef:
                  name: questionario-secrets
                  key: MONGO_DATABASE
            # RabbitMQ aponta pro Service "rabbitmq" (DNS interno do K8s)
            - name: RabbitMQ__HostName
              value: "rabbitmq"
            - name: RabbitMQ__UserName
              valueFrom:
                secretKeyRef:
                  name: questionario-secrets
                  key: RABBITMQ_DEFAULT_USER
            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: questionario-secrets
                  key: RABBITMQ_DEFAULT_PASS
          # Healthcheck - o K8s reinicia o pod se a API travar
          readinessProbe:
            httpGet:
              path: /api/questionario
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/questionario
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15

---

# --- Service ---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: questionario
spec:
  selector:
    app: backend
  ports:
    - port: 5000               # Porta exposta pelo Service
      targetPort: 8080         # Porta do container
      nodePort: 30500          # Porta acessível externamente (30000-32767)
  type: NodePort               # Expõe pra fora do cluster
